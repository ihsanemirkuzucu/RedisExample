@model HashSet<string>
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>
<form asp-controller="SetType" asp-action="Add" method="post" enctype="application/x-www-form-urlencoded">
    <input class="form-control" type="text" name="name" />
    <button class="btn btn-outline-primary" type="submit">Kaydet</button>
</form>

@foreach (var item in Model)
{
    <h1>@item <a class="btn btn-outline-danger" asp-controller="SetType" asp-action="Delete" asp-route-name="@item">Sil</a></h1>
}
